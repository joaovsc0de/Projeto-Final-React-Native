import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  texto: {
    fontSize: 20,
    color: "#fff",
    fontWeight: "bold",
    marginBottom: 20
  },
  drawer: {
    width: "100%",
    height: 85,
    alignItems: "center",
    justifyContent: "center",
  },
  container: {
    flex:1,
    justifyContent: "center",
    alignItems: "center",
  },
});